import { SYMBOL } from "../index";

export class Player {

    name: string;
    symbol: SYMBOL;

    constructor(name: string , symbol: SYMBOL) {
        this.name = name;
        this.symbol = symbol
    }

}
